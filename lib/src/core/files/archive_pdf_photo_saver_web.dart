Future<String> saveArchivePdfFirstPageToPhotos({
  required List<int> pdfBytes,
  required String filename,
}) {
  throw UnsupportedError('Saving archive PDF pages to photos is not supported on web.');
}
